{
  "name": "hardhat-project",
  "scripts": {
    "start": "hardhat node",
    "deploy": "hardhat run ./scripts/deploy.ts --network localhost",
    "addPlanets": "hardhat run ./scripts/addPlanets.ts --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "@nomiclabs/hardhat-ethers": "^2.2.1",
    "hardhat": "^2.11.2"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "ethers": "^5.7.2"
  }
}
